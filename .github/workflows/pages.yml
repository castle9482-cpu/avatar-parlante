name: Deploy to GitHub Pages

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment disabled; Pages is currently served from the gh-pages branch."
